<Page x:Class="Camper.Camper_Log"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Camper"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Camper_Log">

    <Grid>
        <DockPanel LastChildFill="False">
            <DataGrid x:Name="dataGrid" Width="355" d:ItemsSource="{Binding CamperLogs}" Height="343">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="*"/>
                    <DataGridTextColumn Header="Incident" Binding="{Binding Incident}" Width="2*"/>
                </DataGrid.Columns>
                
            </DataGrid>
            <Button Content="Save Logs"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Margin="10"
            Width="100"
            Background="ForestGreen"
            Click="SaveLogs_Click"/>
        </DockPanel>

    </Grid>
</Page>
